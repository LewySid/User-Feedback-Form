# User Feedback Form
This project is an interactive User Feedback Form built using HTML, CSS, and JavaScript. The form collects a user's name, email, and feedback, and uses event-based programming to respond to user actions.
The goal of this project is to practice DOM manipulation, event listeners, event delegation, and form validation.